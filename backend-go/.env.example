# ─────────────────────────────────────────────────────────────────────────────
# backend-go environment variables — EXAMPLE FILE
# Copy to .env and fill in real values before running locally (non-Docker).
#
# NOTE: The Go application currently reads no environment variables.
#       This file exists as a placeholder for future configuration such as:
#         - Database connection strings (when the database service is added)
#         - AI/model service URLs (when the model service is integrated)
#         - JWT secrets (when authentication is implemented)
#
# Example future variables (DO NOT use these values in production):
# ─────────────────────────────────────────────────────────────────────────────

# Server
# PORT=8080

# Database (future)
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=termtemsl
# DB_USER=dbuser
# DB_PASSWORD=changeme

# AI model service (future)
# AI_SERVICE_URL=http://ai-service:5000

# JWT (future)
# JWT_SECRET=changeme
